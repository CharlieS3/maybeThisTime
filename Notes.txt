@types/pg     TypeScript types for pg
tsx           lets you run .ts files directly
typescript    TypeScript itself


To run node:

cd apps/api
npm run start:dev


To run nest:

cd apps/web
npm run dev