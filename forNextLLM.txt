I am building a brewery management tool. Stack: Next.js frontend, NestJS backend, PostgreSQL database using raw SQL.

Important preference: go very slowly, one step at a time. Do not dump many steps at once.

Current repo structure:

code/
  apps/
    web/        # Next.js frontend
    api/        # NestJS backend
  packages/
    database/   # raw SQL migrations/seeds
    shared/     # ignore for now

Backend:
- NestJS API is in apps/api
- It runs on localhost:3000
- GET http://localhost:3000/profiles works
- It returns JSON from Postgres, currently like:
  [
    { id: "...", first_name: "Bruh" },
    { id: "...", first_name: "please work" }
  ]
- POST /profiles exists but does not actually save yet. Ignore POST for now.
- We only want to display existing profile data in Next.js first.

Backend files shown already:

main.ts currently:
import { NestFactory } from '@nestjs/core';
import { AppModule } from './app.module';

async function bootstrap() {
  const app = await NestFactory.create(AppModule);
  await app.listen(process.env.PORT ?? 3000);
}
bootstrap();

profiles.controller.ts has:
@Get()
findAllProfiles() {
  return this.profileServices.findAll();
}

profiles.service.ts has:
async findAll() {
  const result = await this.databaseService.query('SELECT * FROM profiles');
  return result.rows;
}

Frontend:
- Next.js app was created in apps/web
- We pinned create-next-app to Next 16.2.6 because an earlier preview version caused SWC errors
- Tailwind: No
- TypeScript: Yes
- ESLint: Yes
- src directory: No
- App Router: Yes
- Turbopack: No
- Import alias: Yes, @/*
- React Compiler: No
- AGENTS.md: No
- Next dev server works at http://localhost:3001
- package.json dev script was changed to:
  "dev": "next dev -p 3001"

Last confirmed:
- http://localhost:3001 loads the default Next starter page.
- We were about to create:
  apps/web/app/profiles/page.tsx

Next immediate goal:
1. Create a basic /profiles page in Next.js.
2. Confirm http://localhost:3001/profiles loads.
3. Then enable CORS in NestJS for http://localhost:3001.
4. Then fetch GET http://localhost:3000/profiles from the Next page and render profile names.

Please continue from there. If you need more information to continue please let me know.